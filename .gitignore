# Python
__pycache__/
*.py[cod]
.pytest_cache/

# Virtual environment
venv/
.venv/

# Secrets — never commit your real key
.env

# Editor / OS
.DS_Store

# Eval run artifacts (regenerable; contain full bot replies)
evals/results.json
evals/judged.json

# Terraform — never commit state (contains resolved secrets) or tfvars; keep the lock file
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/*.tfvars
terraform/crash.log

# Private job-search materials — NEVER commit (target list, cover letters, salary scripts,
# LinkedIn copy). Personal + strategic; must not appear on the public GitHub repo.
job-kit/
